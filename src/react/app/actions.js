//Custom action code here

export const init = () => {

	/* To add scroll check to menu
	window.addEventListener('scroll', () => {

		if (window.pageYOffset > 0) {
			if(!store.getState().generalState.scrolled) {
				store.dispatch({
					type: 'PAGE_SCROLLED',
					scrolled: true
				})
			}
		} else {
			if (store.getState().generalState.scrolled) {
				store.dispatch({
					type: 'PAGE_SCROLLED',
					scrolled: false
				})
			}
		}
	});
	*/
};

